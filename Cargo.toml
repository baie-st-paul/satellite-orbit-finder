[package]
name = "satelite-orbit-finder"
version = "0.1.0"
edition = "2024"

[dependencies]
reqwest ={ version = "0.12.18", features = ["blocking", "cookies"]}
cookie_store ={ version = "*"}
dotenv = "0.15.0"
